/*Un programa capaz de analizar una frase
número de palabras
longitud media de las palabras
veces cada letra
frecuencia cada letra
Incorporar un interface Web de entrada y salida*/
/**
 * @description: Función que devuelve el numero de palabras de un string
 * @param:{string}
 * @return:{number}
 */


const cadena1= 'Esto es una prueba'

function contarPalabras(cadena) {
    let cadena2 = cadena.split(' ')
    return cadena2.length
    return cadena2
}
    
console.log(contarPalabras(cadena1))


/**
 * @description: Función que devuelve la longitud media de las palabras de una frase 
 * @param {string}
 * @param {array}
 * @return {number}
 */

 function mediaPalabras(cadena) {
     let cadena2 = cadena.split(' ').join()
     let r = 0
     for (let i = 0; i < cadena1.length; i++) {
         const element = array[i];
        r += i
    return r/cadena2.length
     }
     
 }


 console.log(mediaPalabras(cadena1))


 /**
 * @description: Función que devuelve las veces au aparece una letra
 * @param:{string}
 * @return:{number}
 */


 function repeticionLetras(cadena){
     posicion = cadena.indexOf("x");
    while ( posicion != -1 ) {
        suma++
        posicion = cadena.indexOf("x",posicion+1);
    }
}
    
console.log(repeticionLetras(cadena1))

/**
 * @description: Función que devuelve la frecuencia con la que aparece una letra
 * @param:{string}
 * @return:{number}
 */

 


