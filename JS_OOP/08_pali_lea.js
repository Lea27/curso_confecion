/**
 * @description: funcion que dertermine si una cadena de texto es un palindromo
 * @param {string} frase
 * @return  true 
 */

 let frase
 function ispalindromo(frase) {
     let frase = []
     for (let i = 0; i < frase.length; i++) {
         const item = frase[i];
         if ( nombre.toUpperCase() == item.toUpperCase())} else {

         if (frase == frase.reverse()) {
             return true
             
         }
         
     }
     return false
 }

