/**
 * @description: funcion que dertermine si una cadena de texto es un palindromo
 * @param {string} cadena
 * @return  true 
 */

 let frase
 function ispalindromo(cadena) {
     let frase = []
     for (let i = 0; i < frase.length; i++) {
         const item = frase[i];
         if ( nombre.toUpperCase() == item.toUpperCase())

         if (frase == frase.reverse()) {
             return true
             
         }
         
     
     return false
 



 /**
 * @description: comprobar si un string(cadena) es palindromo
 * @param {string} cadena
 * @returns {boolean}
 */
function isPali(cadena) {
    let r = true
    return r   //primero pasos imprescindibles

}}}

